# local Claude Code config / skills / worktrees — not part of the project
.claude/

# internal dev notes — kept locally, not part of the public repo
docs_dev/

# npm auth / local config — never commit
.npmrc

# dependencies
node_modules/

# build output
dist/

# transformers.js model cache (downloaded weights — never commit)
.cache/
.transformers-cache/

# OS / editor
.DS_Store
Thumbs.db
*.log

# generated indexes — rebuilt from data/products.json (npm run demo:index) or at deploy
pragmasearch-index*.json

# local/private datasets (not part of the public sample)
data/products-itshop.json
data/products-10k.json
data/eponuda.xml
demo/.server*.log

# baked model cache (Docker)
.models/
